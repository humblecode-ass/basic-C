#include <stdio.h>

int poww(int x, int y) {
    int temp = 1;
    for(int i = 0; i < y; i++) 
        temp *= x;
    
    return temp;
}
int main() {
    int x, y;
    printf("Nhap x: "); scanf("%d", &x);
    printf("Nhap y: "); scanf("%d", &y);

    printf("%d ^ %d = %d", x, y, poww(x, y));
}
