#include <stdio.h>
int fibo(int x)
{
    if(x==1||x==2)
        return x;
    return fibo(x-1)+fibo(x-2);
}
int main()
{
    int x;
    printf("Nhap x "),scanf("%d",&x);
    printf("So fibo thu %d la %d ",x,fibo(x));
}
