#include <stdio.h>
int main()
{
    int x,y,gcd;
    printf("Nhap so dau tien "),scanf("%d",&x);
    printf("Nhap so thu hai "),scanf("%d",&y);
    if(x<y)
    {
        int temp = y;
        y=x;
        x=temp;
    }
    while (y!=0)
    {
        gcd = y;
        y = x % y;
        x = gcd;
    }
    printf("%d",gcd);
}
