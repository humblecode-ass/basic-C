#include <stdio.h>
int gt(int a)
{
    int y = 1;
    for (int i=a;i!=0;i--)
        y*=i;
}
int main()
{
    int x;
    float sum = 0;
    printf("Nhap so "),scanf("%d",&x);
    for(int i=1;i<=x;i++)
        sum+=1.0/gt(i);
    printf("%.2f",sum);
}
